Election Bot is a Discord bot that finds the results of elections using civicAPI, formats the HTTP GET --> JSON --> Python string, and displays them back to the user who made the command in Discord. 
